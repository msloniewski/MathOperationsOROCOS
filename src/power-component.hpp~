#ifndef OROCOS_MATHOPS_COMPONENT_HPP
#define OROCOS_MATHOPS_COMPONENT_HPP

#include <rtt/RTT.hpp>

class Mathops : public RTT::TaskContext{
  public:
    Mathops(std::string const& name);
    bool configureHook();
    bool startHook();
    void updateHook();
    void stopHook();
    void cleanupHook();
};
#endif
